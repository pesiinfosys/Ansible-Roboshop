---
- name: Test Play
  hosts: web
  become: true
  tasks:
  - name: Process Check
    ansible.builtin.shell: ps -ef | grep {{item}}
    loop:
    - apache
    register: ps_out

  - name: Print OutPut
    ansible.builtin.command: echo "{{ps_out}}"

